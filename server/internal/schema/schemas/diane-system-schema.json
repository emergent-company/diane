[
  {
    "type_name": "AgentOverrideConfig",
    "description": "Partial overrides for built-in agent definitions. Only specified fields override the Go-code defaults at seed time.",
    "namespace": "system",
    "json_schema": {
      "label": "Agent Override Config",
      "type": "object",
      "properties": {
        "agent_name": {
          "type": "string",
          "description": "Built-in agent name to override (e.g. diane-default)"
        },
        "system_prompt": {
          "type": "string",
          "description": "Override system prompt (empty = keep built-in)"
        },
        "skills": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Override skills list (empty = keep built-in)"
        },
        "model_provider": {
          "type": "string",
          "description": "Override model provider (empty = keep built-in)"
        },
        "model_name": {
          "type": "string",
          "description": "Override model name (empty = keep built-in)"
        },
        "model_temperature": {
          "type": "number",
          "description": "Override model temperature (0 = keep built-in)"
        },
        "model_max_tokens": {
          "type": "integer",
          "description": "Override model max tokens (0 = keep built-in)"
        },
        "max_steps": {
          "type": "integer",
          "description": "Override max steps (0 = keep built-in)"
        },
        "timeout": {
          "type": "integer",
          "description": "Override timeout in seconds (0 = keep built-in)"
        },
        "visibility": {
          "type": "string",
          "description": "Override visibility (project/org/private, empty = keep built-in)"
        },
        "sandbox_enabled": {
          "type": "boolean",
          "description": "Override sandbox enabled state (null = keep built-in)"
        }
      },
      "description": "Maps agent names to partial override fields applied at seed time"
    },
    "enabled": true
  },
  {
    "type_name": "SkillMonitorCheckpoint",
    "description": "Tracks the last time diane-skill-monitor checked completed sessions for skill extraction",
    "namespace": "system",
    "json_schema": {
      "label": "Skill Monitor Checkpoint",
      "properties": {
        "last_check": {
          "type": "string",
          "description": "ISO 8601 timestamp of the last monitor run"
        },
        "agent_name": {
          "type": "string",
          "description": "The agent this checkpoint tracks runs for (e.g. diane-default)"
        }
      },
      "description": "Tracks skill monitor checkpoints for periodic session review"
    },
    "enabled": true
  },
  {
    "type_name": "DianeNodeConfig",
    "description": "Diane node configuration shared across project instances. Each node creates/updates its config on startup for discovery and mode identification.",
    "namespace": "system",
    "json_schema": {
      "label": "Diane Node Config",
      "type": "object",
      "properties": {
        "instance_id": {
          "type": "string",
          "description": "Unique instance identifier for this node (e.g. mcj-mini, tool-mac)"
        },
        "hostname": {
          "type": "string",
          "description": "Machine hostname from os.Hostname()"
        },
        "mode": {
          "type": "string",
          "description": "Node operational mode: master (runs Discord bot, manages agents) or slave (MCP relay only)",
          "enum": ["master", "slave"]
        },
        "version": {
          "type": "string",
          "description": "Diane software version running on this node"
        },
        "last_seen": {
          "type": "string",
          "description": "ISO 8601 timestamp of the last node check-in / relay connection"
        }
      },
      "description": "Diane node configuration shared across project instances"
    },
    "enabled": true
  },
  {
    "type_name": "AgentToolConfig",
    "description": "Agent tool access configuration — maps agent names to tool glob patterns. When present, these patterns are merged into the agent definition's tools list on every seed/sync.",
    "namespace": "system",
    "json_schema": {
      "label": "Agent Tool Config",
      "type": "object",
      "properties": {
        "agent_name": {
          "type": "string",
          "description": "Agent definition name (e.g. diane-default, diane-researcher)"
        },
        "tool_patterns": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Tool glob patterns to add (e.g. [\"*github*\", \"*slack*\", \"*notion*\"])"
        }
      },
      "description": "Maps agent names to tool glob patterns merged on seed"
    },
    "enabled": true
  }
]
